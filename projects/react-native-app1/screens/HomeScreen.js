import React from "react";
import { View } from "react-native";

import { BottomTabsNavigation } from "../navigation";

const HomeScreen = ({ navigation }) => {
  return <BottomTabsNavigation navigation={navigation} />;
};

export default HomeScreen;
